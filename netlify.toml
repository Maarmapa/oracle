[functions]
  node_bundler = "esbuild"
  included_files = ["app/api/**"]

[functions."api/avatar/session"]
  timeout = 60

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
